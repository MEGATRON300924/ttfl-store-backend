const fs=require("fs");const path=require("path");const schemaPath=path.join(process.cwd(),"prisma","schema.prisma");let schema=fs.readFileSync(schemaPath,"utf8");
function addField(modelName,marker,fieldName,fieldDefinition){const modelStart=schema.indexOf(`model ${modelName} {`);if(modelStart===-1)return;const modelEnd=schema.indexOf("\n}",modelStart);const end=modelEnd===-1?schema.length:modelEnd;const modelBlock=schema.slice(modelStart,end);if(modelBlock.includes(`  ${fieldName} `))return;const markerIndex=schema.indexOf(marker,modelStart);if(markerIndex===-1||markerIndex>end)return;schema=schema.slice(0,markerIndex)+`  ${fieldDefinition}\n`+schema.slice(markerIndex);}
addField("Product","  createdAt","publicProductId","publicProductId String @unique @default(cuid())");addField("Product","  createdAt","estimatedDeliveryDays","estimatedDeliveryDays Int @default(7)");addField("VendorOrder","  items               OrderItem[]","trackingEvents","trackingEvents       TrackingEvent[]");
if(!schema.includes("model TrackingEvent {")){schema+=`\n\nmodel TrackingEvent {\n  id             String      @id @default(uuid())\n  vendorOrderId  String\n  vendorOrder    VendorOrder @relation(fields: [vendorOrderId], references: [id], onDelete: Cascade)\n  checkpoint     Int\n  title          String\n  description    String?\n  avatar         String      @default("package")\n  trackingUrl    String?\n  riderName      String?\n  riderPhone     String?\n  createdAt      DateTime    @default(now())\n\n  @@unique([vendorOrderId, checkpoint])\n  @@index([vendorOrderId])\n  @@index([createdAt])\n  @@map("tracking_events")\n}\n`;}
fs.writeFileSync(schemaPath,schema);console.log("Prisma schema prepared with safe product IDs, delivery estimates, and order tracking fields.");
